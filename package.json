{
  "name": "horizm-pilot",
  "version": "0.1.0",
  "private": true,
  "description": "Sponsor exposure measurement pilot: Next.js front end, Fastify API, shared contracts.",
  "scripts": {
    "setup": "npm --prefix contracts install && npm --prefix api install && npm --prefix web install && npm --prefix api run db:push && npm --prefix api run seed",
    "dev": "node scripts/dev.mjs",
    "dev:api": "npm --prefix api run dev",
    "dev:web": "npm --prefix web run dev",
    "seed": "npm --prefix api run seed",
    "test": "npm --prefix api test"
  }
}
