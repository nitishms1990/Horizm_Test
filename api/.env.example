# Copy to .env before running the API.
DATABASE_URL="file:./dev.db"

# Signs the session cookie. Any long random string; change it before hosting.
COOKIE_SECRET="pilot-only-secret-change-before-hosting"

PORT=4000

# Model behind the analyst agent. Sonnet is quick and reads the tools' JSON well.
HORIZM_AGENT_MODEL="sonnet"
